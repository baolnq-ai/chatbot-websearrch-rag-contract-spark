# Operations Docs

Thư mục này lưu tài liệu vận hành cho môi trường local và triển khai thật.

Nội dung chính gồm runbook start/stop, checklist xử lý sự cố, cấu hình port trong dải `6100-6150`, cách chạy `setup.sh`, cách dừng bằng `stop.sh` và ghi chú riêng cho hạ tầng GB10/DGX Spark.
