# Task Logs

Thư mục này lưu log theo từng task triển khai, kiểm thử hoặc cập nhật tài liệu của dự án.

Các log thường được chia theo năm trong thư mục con như `2026/`. Mỗi log ghi ngắn gọn mục tiêu, diễn biến chính, lệnh verify, kết quả, blocker và rủi ro còn lại.
