# Testing Logs

Thư mục này lưu log các phiên kiểm thử theo năm.

Mỗi log ghi mục tiêu, lệnh đã chạy, kết quả chính, evidence liên quan, blocker và rủi ro còn lại. Với test có ảnh/video/GIF, log phải trỏ tới artifact trong `test/`, `tests/` hoặc `docs/assets/` để reviewer xem được trên GitHub.
