# API evidence

Thư mục này lưu kết quả smoke API ở dạng JSON và HTML render được trên GitHub/browser.

Report chỉ ghi endpoint, method, status, thời lượng và mô tả kết quả. Không ghi cookie, token, password hoặc payload nhạy cảm.
